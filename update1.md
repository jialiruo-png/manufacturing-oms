## 2026-06-19

### 目的

将公开演示环境中的原客户品牌露出替换为虚构制造企业品牌，避免 GitHub Demo 首页继续显示原客户相关信息。

### 操作

前端登录页、顶部品牌栏、移动端登录页和移动端顶部栏统一改为「恒锐智造 / HRM MFG」，并新增公开演示用 `logo.svg` 替换原登录页 logo 引用。

### 预期效果

用户打开 Demo 时看到的是中性的模板品牌，不再直接暴露原项目客户名称、缩写和柴油机品牌信息。

## 2026-06-19

### 目的

优化公开 Demo 登录页首屏观感，去掉临时生成但视觉效果不佳的品牌图片。

### 操作

删除登录页顶部 logo 图片引用，并移除公开目录中的 `logo.svg`，保留纯文字品牌「HRM MANUFACTURING」。

### 预期效果

登录页顶部更简洁，不再显示粗糙的临时 logo，公开演示的视觉观感更稳定。

## 2026-06-19

### 目的

根据公开 Demo 首页观感调整登录页欢迎语，让产品定位更突出 AI 订单协同。

### 操作

将登录页欢迎语改为「欢迎登陆恒锐AI订单协同管理系统」。

### 预期效果

首页副标题更简洁，能直接传达系统是面向订单协同管理的 AI 化 Demo。

## 2026-06-19

### 目的

彻底清理前端公开资源中的原客户品牌残留，避免未引用的旧 logo 仍随静态资源发布。

### 操作

全量扫描 `frontend` 目录中的原客户可见品牌文案，并删除不再使用的 `frontend/public/logo.jpg`。

### 预期效果

除内部历史类名和存储 key 外，前端页面与公开静态资源不再出现原客户品牌露出。

## 2026-06-19

### 目的

避免订单列表、详情和后续新建订单中通过合同号显示原客户缩写。

### 操作

将后端合同号生成前缀和 Prisma demo 种子数据中的订单号改为 `HRM`，并同步更新相关解析测试样例。

### 预期效果

重置 demo 数据或新建订单后，前端页面展示的合同号统一使用 `HRM` 前缀。

## 2026-06-19

### 目的

全面排查业务员、用户、客户和订单相关字段中的真实姓名露出，避免表格、下拉框、看板排行和详情页继续显示真实人员信息。

### 操作

新增接口响应脱敏中间件，将两个真实姓名统一替换为虚拟姓名「林嘉宁」「周启明」；新增 `npm run db:anonymize-names` 数据库脱敏脚本，覆盖 `User`、`Customer`、`Order`、`CommLog`、`ApprovalLog`、`PasswordResetRequest` 等会流入前端展示的姓名字段；同时移除 README 中含真实标识的联系邮箱。

### 预期效果

即使历史数据库仍有残留真实姓名，API 返回给前端前也会统一转成虚拟姓名；执行数据库脚本后，持久化数据本身也会完成脱敏。

## 2026-06-19

### 目的

明确处理已写入生产数据库的历史真实姓名，避免只完成代码脱敏但前端仍从存量数据中看到真实姓名。

### 操作

新增 `deploy/aliyun/anonymize-real-names.sql`，用 PostgreSQL Unicode escape 写法定位两个敏感姓名并更新 `User`、`Customer`、`Order`、`CommLog`、`ApprovalLog`、`PasswordResetRequest` 中会展示到前端的历史字段。

### 预期效果

运维在 ECS 或可访问 RDS 的机器上执行 SQL 后，生产库历史订单、业务员列表、看板排行和详情页中的真实姓名会被持久化替换为虚拟姓名。
