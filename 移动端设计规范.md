# 移动端设计规范

> 适用范围：制造业 OMS（YMT DIESEL）H5 移动端适配（mobile/ 目录）
> 配套：`plan7.md`、PC 端 `frontend/src/index.css`、`frontend/src/pages/*.css`
> 设计原则：**完全沿用 PC 视觉语言**，仅针对触屏与小屏做必要调整
> 最后更新：2026-05-16

---

## 0. 总纲

- 品牌色、圆角、阴影、状态色板、AI 按钮渐变等全部继承 PC 端，不重新发明
- 字号梯度、间距体系、触控目标按本规范执行，凡 PC 端数值不适合手指操作的一律放大到下文规范
- 任何与本文件冲突的临时改动，必须先回头更新本文件再实施

---

## 1. 字号梯度（rem 基准 16px）

| 用途 | 字号 | 字重 | 备注 |
|------|------|------|------|
| 页面标题 | 20px | 700 | MTopBar 居中标题、Drawer 顶部标题 |
| 卡片主标题 | 16px | 600 | 订单号、客户名等卡片首要信息 |
| 正文 | 14px | 400–500 | 列表正文、按钮文字、表单值 |
| 辅助文字 | 12px | 400 | 交期、辅助标签、时间戳，颜色 `#94a3b8` |
| 数字/金额 | 15px | 600 | 必须 `font-variant-numeric: tabular-nums` 等宽对齐 |

> PC 端 13px 正文在手机上偏小，移动端**强制提到 14px**；其余字号比例保留 PC 体系。

---

## 2. 间距体系（4px 栅格）

| 场景 | 数值 |
|------|------|
| 屏幕左右边距 | 16px（< 390px）/ 20px（≥ 390px） |
| 卡片内边距 | 14px（上下） × 16px（左右） |
| 卡片间距（同组） | 10px |
| 卡片间距（跨分组） | 16px |
| 卡片内行间 | 8px |
| 卡片内段落间 | 12px |
| TopBar 与首个卡片 | 12px |

---

## 3. 触控目标

| 元素 | 最小尺寸 |
|------|----------|
| 普通按钮 | min-height 44px，`border-radius: 8px`（沿用 PC 圆角） |
| 主要操作按钮 | 48px |
| 图标按钮 | 40 × 40px，图标本身 20px |
| 列表行 | min-height 56px（足够承载双行信息） |
| 底部 Tab Bar 槽位 | 64px × (整宽 / 5) |
| 表单输入框 | 44px，内边距 12px × 14px |

> 任何小于 44px 的可点击区域必须在外层包一个 44 × 44px 的不可见命中区域。

---

## 4. 卡片设计（PC 表格行 → 移动卡片）

### 4.1 结构

```
┌─────────────────────────────────────┐
│ 主信息（订单号/客户名） │ 状态徽章 │ ← 上行
│ 次信息（产品摘要 …） 省略号截断    │ ← 中行
│ 12px 灰 时间/操作          查看 ›  │ ← 下行
└─────────────────────────────────────┘
```

### 4.2 视觉细节

- **状态徽章**：完全复用 PC `ymt-status-*` 色板（draft / pending_approval / procurement / production / pending_ship_approval / ready_ship / shipped / rejected），圆角胶囊（高 22px），固定在卡片右上
- **紧急 / 警告 / 完成色条**：保留 PC `order-card-red / order-card-amber / order-card-green` 设计，作为卡片**左侧 3px 色条**
- **可点击整卡**：整张卡片是触控目标；末行右侧用"查看详情 ›"（12px 灰 + 右箭头）暗示可点击，**避免误点**
- **金额、进度、剩余天数等数字**：右对齐、`tabular-nums`，永远固定在卡片右侧形成可扫读的列

### 4.3 信息密度

PC 端 9-10 列表格在移动端转卡片后，**最多展示 5 个字段**：

1. 主信息（订单号 / 客户名）
2. 状态徽章
3. 次信息（摘要 / 产品 / 联系人）
4. 关键数值（金额 / 进度 / 物料就绪比）
5. 时间（交期 / 创建时间）

> 其余字段进**详情页**，不在列表卡片显示。

---

## 5. 底部 Tab Bar

| 项 | 规范 |
|---|---|
| 高度 | 56px + `env(safe-area-inset-bottom)` |
| 底色 | `rgba(255, 255, 255, 0.96)` + `backdrop-filter: blur(20px) saturate(180%)` |
| 顶部分隔 | 1px `#e8edf5` |
| 图标 | 22px，使用与 PC 同款 antd icons |
| 标签文字 | 10px，激活态 600 |
| 激活色 | `#c4000b`（PC YMT 红） |
| 未激活色 | `#8a94a6`（PC 同款灰） |
| 激活态指示条 | 顶部 2px 红色短指示条，宽 24px，居中（移植 PC `app-main-tabs ink-bar`） |

### 5.1 红 chip 徽标（待办计数）

- **圆点徽标**（无数字）：`8 × 8px`，背景 `#ef4444`，2px 白描边，位于图标右上 `top: -4px; right: -4px`
- **数字徽标**：`min-width: 16px; height: 16px; padding: 0 4px; border-radius: 999px; background: #ef4444; color: #fff; font-size: 10px; font-weight: 700; border: 2px solid #fff`，> 99 显示 `99+`

---

## 6. 顶部 TopBar

| 场景 | 高度 |
|------|------|
| 一级页面（含底部 Tab Bar） | 48px + `env(safe-area-inset-top)` |
| 二级页面（详情 / Drawer 内页） | 56px + `env(safe-area-inset-top)` |
| 含搜索 / 筛选条 | 64px + `env(safe-area-inset-top)` |

### 6.1 视觉

- **底色** `rgba(255, 255, 255, 0.96)` + `backdrop-filter: blur(20px) saturate(180%)`（与 PC `app-top-nav` 一致）
- **底部分隔** 1px `#e8edf5`
- **左侧** 40 × 40 返回箭头；一级页面替换为品牌"YMT DIESEL"（红 + 深灰，与 PC `app-brand` 一致）
- **中间** 标题 16px / 700 居中
- **右侧** 最多 2 个 40 × 40 图标按钮（刷新、铃铛 / 筛选）
- 滚动行为：**保持不变**，不做收起动画，避免抖动

---

## 7. 安全区（iOS 刘海 / 灵动岛 / 底部 Home Indicator）

```css
/* TopBar */
padding-top: env(safe-area-inset-top);

/* 底部 Tab Bar */
padding-bottom: env(safe-area-inset-bottom);

/* 内容区底部需为 Tab Bar 留位 */
padding-bottom: calc(56px + env(safe-area-inset-bottom) + 16px);

/* 横屏刘海 */
padding-left: env(safe-area-inset-left);
padding-right: env(safe-area-inset-right);
```

`<meta name="viewport">` 需加 `viewport-fit=cover`：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
```

---

## 8. 滚动与回弹

```css
.m-scroll-container {
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  overscroll-behavior-y: contain;
}
```

- 列表底部用"已加载全部"小灰字 12px 收尾，**禁用无限弹簧**
- 下拉刷新基于原生 `touchstart/touchmove/touchend` 自实现（plan7 `usePullRefresh.ts`），阈值 60px；下拉指示器固定在 TopBar 下方，**不要顶起整个内容**

---

## 9. 表单与键盘

### 9.1 布局

- 整页 `min-height: 100dvh` 替代 `100vh`，适配 iOS 软键盘
- 表单底部主按钮使用 `position: sticky; bottom: 0; padding-bottom: env(safe-area-inset-bottom)` + 半透明白底 + 顶部 1px 分割线；键盘弹起时按钮被键盘**顶上去而非遮挡**

### 9.2 输入

- 输入框 focus 时调用 `scrollIntoView({ block: 'center', behavior: 'smooth' })` 兜底
- 数字输入使用 `inputMode="numeric"`，电话用 `inputMode="tel"`，避免弹中文键盘
- Drawer 表单关闭时主动 `document.activeElement?.blur()`，防止键盘残留

### 9.3 提交

- 提交按钮在 loading 期间禁用并显示 PC 同款 spinner
- 表单错误使用 antd `Form.Item.help` 红色文字，不弹 toast，避免遮挡输入

---

## 10. 风格"沿用 PC"的关键继承点

| 项 | 值 / 来源 |
|---|---|
| 主红 | `#c4000b`（品牌 YMT 红） |
| 主蓝 | `#2f66ff` / `#2563eb`（按钮、链接） |
| 页面底色 | `rgb(247, 247, 247)` |
| 圆角 | 8px（按钮、卡片、输入框） |
| 胶囊圆角 | 999px（徽章、状态 tag、Tab 指示条） |
| 卡片阴影 | `0 1px 3px rgba(0, 0, 0, 0.07)`（与 PC 一致，**不加重**） |
| AI 按钮 | 完全保留 `ymt-ai-btn` 渐变紫与跑光动画；移动端高度 36px、字号 13px |
| 状态色 | 完全复用 PC `ymt-status-*` 八种组合 |
| 字体栈 | 沿用 PC 已声明的字体栈（含 PR1 引入的 Web Font），不在移动端单独声明 |

---

## 11. 不允许的做法（黑名单）

- ❌ 重新发明品牌色或调整 YMT 红色色值
- ❌ 加深卡片阴影、加大圆角到 12px 以上
- ❌ 引入新的字体（除 PR1 已确定的 Noto Sans SC / Inter / Nunito / JetBrains Mono）
- ❌ 引入任何手势库（react-use-gesture / hammer.js 等）
- ❌ 引入 antd-mobile（移动端继续用 antd 5）
- ❌ 引入 Service Worker / manifest.json（不做 PWA）
- ❌ 给微信浏览器写 UA 分支
- ❌ 数据看板做模块裁剪、模块合并、字段隐藏
- ❌ 业务员页面隐藏 Excel 上传 + AI 智能解析入口
- ❌ 用横向滚动表格代替卡片流（除生产 BOM 子表等极少数确实表格更易读的场景，且必须 < 5 列）

---

## 12. 验收清单

每个移动端页面合并前必须满足：

- [ ] 所有按钮 height ≥ 44px，列表行 height ≥ 56px
- [ ] 顶部 / 底部安全区已用 `env(safe-area-inset-*)` 处理
- [ ] 长文字省略号截断，不出现换行抖动
- [ ] 金额 / 数量数字使用 `tabular-nums`，多行对齐
- [ ] 状态徽章颜色来自 PC `ymt-status-*` 色板
- [ ] 整卡可点击区域不与卡片内小按钮冲突
- [ ] 键盘弹起后主按钮不被遮挡
- [ ] iPhone 14 Pro / Pixel 7 / 鸿蒙 Mate 60 三种 viewport 下截图无破版
- [ ] 切换为 PC 宽度（> 768px）后自动 reload 进入 PC 版，PC 版无视觉变化
